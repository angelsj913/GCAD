GCAD Formal Model Verification Record
======================================

Date: 2026-09-14
Verifier: Dafny 4.11.0
Command:
  .mcp-tools\dafny\dafny\Dafny.exe verify formal\invariants.dfy

Result:
  Dafny program verifier finished with 46 verified, 0 errors

Modelled properties:
  - Shannon-entropy accumulation recursion terminates for a bounded histogram.
  - Snapshot ring capacity and head/count bounds are preserved.
  - XOR canary round-trip preserves the original value.
  - Blocked-IP operations preserve uniqueness.
  - Threat level ordering is strictly monotonic.
  - EventLog::Push retains at most max_events entries, including max_events == 1.
  - ThreadPool model preserves queued-plus-active work accounting through
    enqueue, worker start/finish, queue clearing, close, and idle detection.
  - Scan progress never exceeds its declared total, including after cancel.
  - Recent event IDs are allocated monotonically and the bounded window keeps
    the newest appended event.
  - The PMSR shadow-store model bounds intended public state to two ring
    capacities; this is an abstraction, not a proof of vector behavior.

Scope and limits:
  - This is a Dafny model, not a proof of the corresponding C++ implementation.
  - The entropy model uses an uninterpreted Log2 declaration only to model
    accumulation shape. It does not prove numeric entropy bounds.
  - This record does not prove that live engine locks are deadlock-free, that
    the C++ program is data-race-free, or that all allocations are leak-free.
  - Those claims require code review, runtime tests, and appropriate dynamic
    analysis; an unavailable formal tool is never treated as a passing result.
